#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strlen - returns the length of a string len.
 * @s: string
 * Return: len of list.
 */
int _strlen(char *s)
{
int len = strlen(s);
return (len);
}
