#include <stdio.h>
/**
 * main - Prints numbers from 0 to 9.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int letter;
for (letter = 0; letter <= 9; letter++)
{
printf("%d ", letter);
}
printf("\n");
return (0);
}
